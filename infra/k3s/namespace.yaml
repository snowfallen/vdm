# ============================================================
# Namespace — ізоляція проекту vdm.
# ============================================================
apiVersion: v1
kind: Namespace
metadata:
  name: vdm
  labels:
    app: vdm-shop
