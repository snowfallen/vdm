package vdm.shop.model.enumeration;

public enum AttributeDataType {
    DICT, // вибір з готових опцій (селект)
    TEXT, // довільний текст (Комплектація, Властивості)
    NUMBER // число з одиницею виміру (мм, кг, W)
}
